name: Deploy

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Building the website
      run: |
        ls
        yarn global add gatsby-cli
        yarn install
        yarn run deploy
